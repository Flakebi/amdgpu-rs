[package]
name = "vector_add"
version = "0.1.0"
edition = "2024"

# TODO Can add in proc-macro?
[lib]
crate-type = ["cdylib"]
path = "src/main.rs"

[features]
gpu = []

# TODO Remove if possible
[build-dependencies]
amdgpu-device-libs-build = "0.1"

[dependencies]
gpu-kernel = { path = "../.." }
